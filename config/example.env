# Exemplo de configuração por canal
# Copie este arquivo para config/<nome-do-canal>.env e ajuste os valores conforme necessário.

# Identificador do canal (opcional; usado apenas para referência)
# CHANNEL_NAME=canal_teste

# Chave da live (em vez de definir STREAM_URL diretamente)
# YOUTUBE_STREAM_KEY=xxxx-xxxx-xxxx-xxxx

# URL RTMP completa (use em vez da chave se preferir)
# STREAM_URL=rtmp://a.rtmp.youtube.com/live2/xxxx-xxxx-xxxx-xxxx

# Servidor RTMP do YouTube (pode ser a, b, x, etc.)
# YOUTUBE_RTMP_BASE=rtmp://x.rtmp.youtube.com/live2

# Caminho para um arquivo contendo a chave (uma linha só, sem quebras) dentro do container
# STREAM_KEY_FILE=/app/keys/canal_teste.key

# Diretório com os arquivos MP3
# MP3_DIR=/app/musicas

# Diretório com os vídeos (opcional, padrão: /app/video)
# VIDEO_DIR=/app/video

# Caminho para o vídeo específico (opcional)
# Se definido, este vídeo será sempre usado (ignora a busca automática)
# Se não definido, o sistema procura vídeos com o padrão: ${CHANNEL_NAME}_*.mp4
# Exemplo: para canal "cozy", procura por cozy_1.mp4, cozy_2.mp4, etc.
# Se não encontrar, tenta o padrão antigo: ${CHANNEL_NAME}.mp4
# VIDEO_FILE=/app/video.mp4

# URL para baixar o vídeo no startup (opcional)
# VIDEO_SOURCE_URL=https://example.com/meu_video.mp4
# VIDEO_DOWNLOAD_RETRIES=3
# VIDEO_DOWNLOAD_TIMEOUT=300

# Qualidade do vídeo
# VIDEO_SCALE=1920:1080
# VIDEO_FPS=30
# VIDEO_PRESET=superfast
# VIDEO_BITRATE=4500k
# VIDEO_MAXRATE=5500k
# VIDEO_BUFSIZE=8000k
# GOP_SIZE=60
# VIDEO_FILTER_EXTRA=

# Áudio
# AUDIO_BITRATE=160k
# AUDIO_SAMPLE_RATE=44100

# Controle de reinício da live
# Hora do dia para encerrar e reiniciar a live (formato: HH, ex: 12 para meio-dia)
# Isso garante que cada live tenha exatamente 24 horas de duração
# RESTART_HOUR=12

# Outras opções
# ENFORCE_CBR=1
# FFMPEG_THREADS=2
# FORCE_SQUARE_PIXELS=1
# ENABLE_SERVER=1

